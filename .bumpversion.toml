[tool.bumpversion]
current_version = "0.1.0"
parse = "(?P<major>\\d+)\\.(?P<minor>\\d+)\\.(?P<patch>\\d+)"
serialize = ["{major}.{minor}.{patch}"]
commit = true
tag = true
tag_name = "v{new_version}"
tag_message = "Release v{new_version}"
commit_message = "chore: bump version {current_version} → {new_version}"

# Python package version in pyproject.toml
[[tool.bumpversion.files]]
filename = "pyproject.toml"
search = 'version = "{current_version}"'
replace = 'version = "{new_version}"'

# Python __version__ in package __init__.py
[[tool.bumpversion.files]]
filename = "src/celery_flow/__init__.py"
search = '__version__ = "{current_version}"'
replace = '__version__ = "{new_version}"'

# Frontend package.json version
[[tool.bumpversion.files]]
filename = "src/celery_flow/server/ui/frontend/package.json"
search = '"version": "{current_version}"'
replace = '"version": "{new_version}"'
